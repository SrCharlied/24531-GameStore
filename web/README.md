# Frontend (placeholder)

Carpeta reservada para el frontend React. Se inicializará en la Fase 2 del plan (`fases-v2.md`):

```bash
npm create vite@latest . -- --template react
npm install axios react-router-dom
```

Por ahora está vacía a propósito — el servicio `web` aún no está habilitado en `docker-compose.yml`.
